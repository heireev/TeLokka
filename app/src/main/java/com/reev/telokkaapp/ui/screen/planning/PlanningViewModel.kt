package com.reev.telokkaapp.ui.screen.planning

import androidx.lifecycle.ViewModel
import com.reev.telokkaapp.data.PlaceRepository

class PlanningViewModel(private val repository: PlaceRepository): ViewModel() {
}